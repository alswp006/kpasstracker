# 앱인토스 제출 패키지 — KPassTracker

> 앱인토스 콘솔 검수 제출 가이드. 콘솔 등록·검수 요청은 수동 단계입니다.

> ⚡ **MCP로 하면 더 빠릅니다** → `artifacts/submission/CONSOLE-MCP.md` (단계마다 값이 박힌 복붙용 프롬프트 · 되돌릴 수 없는 지점은 4단계 하나)

## 핵심 정보
- **appName(콘솔 등록명과 반드시 일치)**: `kpasstracker` ← 불일치 시 배포/제출 4031 에러
- 표시 이름(displayName): KPassTracker
- 카테고리: 생활 (매니페스트 값) — 2026-08-27 **'재테크' 카테고리가 신설**됐습니다(가계부·소비 분석·절약 챌린지·저축 습관 관리·투자 기록·모의 투자·경제/금융 지식 — 전용 탭에서 관심 유저에게 노출). 바꾸려면 콘솔: 앱 관리 → 앱 정보 수정 → 카테고리.
- 번들 크기: **미측정** (ait build 미실행 — 100MB 이하 필수, 콘솔 업로드 전 직접 확인 필요)
- 배포 URL: (미배포 — ait deploy 후 갱신)
- 테스트 스킴: (미배포 — ait deploy 후 발급)

## A. 지금 등록 (3필드 · 1분)

최초 등록은 **3필드뿐**입니다(2026 등록 간소화). 카테고리·부제·설명·키워드·로고·스크린샷·약관은 B절 — **출시 전**에 채웁니다.

1. **앱 이름**(등록 후 수정 가능): KPassTracker
2. **appName**(⚠️ **등록 후 수정 불가**): `kpasstracker` — 위 핵심 정보의 값과 **정확히 일치**해야 합니다
3. **앱 유형**: 비게임 (매니페스트 값)

> 이 등록이 `ait deploy`의 **전제조건**입니다 — 등록 전에 배포하면 **4031**입니다.

## B. 출시 전 (검수 요청까지)

등록을 마친 뒤, **검수 요청 전까지** 채우는 것들입니다.

1. 콘솔 폼 필드 입력 — `artifacts/apps-in-toss-manifest.txt`의 값을 복사
2. 로고/배너 업로드 — `artifacts/logo.png`, `artifacts/logo-dark.png`, `artifacts/thumbnail-wide.png`
3. 스크린샷 업로드 — `artifacts/screenshots/` 4장(세로 3·가로 1) (콘솔 규격 636×1048 / 1504×741 · 콘솔 최소: 세로 3장 + 가로 1장) — **후보입니다**: 자리 페이지·미완성 화면이 섞였을 수 있으니 업로드 전 눈으로 확인하세요
4. 약관/법적 체크박스 동의 (수동) — 콘솔이 요구하는 항목 전부, **IAA/IAP 개정 약관 포함**
5. 검수 요청 제출

## C. 배포 후 콘솔 설정 (수익·유통)

여기부터는 `ait deploy` **후**에 콘솔에서 하는 설정입니다 — 제출 **전**(A·B절)과 시점이 다릅니다.

1. **핵심 지표 → 활성 지표 정의**(재방문·체류시간·방문 횟수) — 미설정이면 토스가 활성 유저의 **유사 유저에게 앱을 추천**하지 않습니다(광고비를 안 쓰는 이 공장의 **유일한 무료 유통 스위치**가 꺼진 채입니다). **전환 지표 템플릿도 재선택 후 저장**하세요(일부 항목이 활성 지표로 이관됐습니다). 변경은 **다음 날부터** 적용됩니다.
2. **인앱 광고 분석** — 요청→수신→노출시도→노출성공 4단 퍼널과 유형별 **eCPM**을 확인합니다. **SDK 2.7.0 이상**이어야 하고, 업데이트 **이후** 데이터부터 수집됩니다(그 전 구간은 비어 있는 게 정상입니다).
3. **광고 그룹/슬롯 ID 발급 후 env 주입 재빌드** — 발급값은 빌드 시점에 앱에 박힙니다. 콘솔에서 발급만 받고 끝내면 앱은 그 값을 모릅니다 — **env 주입 후 재빌드·재배포**가 필요합니다.
4. **구독(IAP 정기 결제)을 쓴다면 webhook URL 등록**(필수) — **샌드박스가 없으므로** 실제 결제로 확인해야 합니다.
5. **사용자 정보 불러오기(토스 로그인)를 쓴다면** 콘솔에서 동의 화면을 설정하고 코드를 발급받으세요(한 번 요청에 **최대 5개** 항목). — 다만 **정말 필요한지 먼저 확인하세요**: `anonKey`는 서버 연동·동의 절차 **없이** 미니앱 안에서 바로 발급되고(미니앱마다 고유 · 같은 사용자는 항상 같은 값 · 검증 API 제공) 스마트발송·프로모션·토스페이에 그대로 쓸 수 있습니다. 토스 로그인이 필요한 경우는 **본인 확인·중복가입 방지처럼 앱인토스 밖 계정과 같은 사람인지 연결해야 할 때뿐**이고, 동의 화면 이탈률이 **절반에 가까운** 미니앱도 있었습니다(2026-07-23).

## 검수 체크리스트 (제출 전 자가 점검)

대괄호는 **어디서 하는 일인지**입니다 — `[등록 폼]` A절 · `[출시 전 폼]` B절 · `[앱 코드]` 레포(고치려면 재빌드) · `[계정 약관]` 콘솔 계정 설정.

- [ ] [등록 폼] 앱 이름(displayName)에 **한글 포함** — 콘솔 필수 규격(v3부터 config가 아니라 콘솔에서 입력)
- [ ] [출시 전 폼] 앱 **아이콘 업로드** — 콘솔 필수(v3부터 config icon 필드 없음)
- [ ] [출시 전 폼] 만 19세 이상 대상 — 미성년자 타겟 콘텐츠 없음(연령등급은 출시 전 폼에서 선택)
- [ ] [앱 코드] 외부 이탈(outlink)/앱 설치 유도 없음 — 모든 흐름 앱 내부
- [ ] [앱 코드] TDS 컴포넌트만 사용 (shadcn/MUI 등 금지)
- [ ] [앱 코드] 프로덕션 빌드 console.error 0건
- [ ] [앱 코드] 외부 로깅/분석(GA 등) 없음 — SDK Analytics만
- [ ] [앱 코드] HEX 하드코딩 없음 (다크모드 대응 — TDS 토큰/CSS 변수)
- [ ] [앱 코드] AI 결과물 노출 시 생성형 AI 고지 1회 + 라벨 표시
- [ ] [앱 코드] **광고/결제를 쓰는 앱만** — 슬롯 ID는 콘솔 발급값이며 **템플릿이 자동 주입하지 않습니다**(`VITE_TOSS_AD_GROUP_ID`/`VITE_TOSS_AD_SLOT_ID`·`IAP_SKU`를 직접 env로 넣고 **재빌드**)
- [ ] [계정 약관] 약관/법적 체크박스 동의 — 콘솔이 요구하는 항목 전부(**IAA/IAP 개정 약관 포함** — IAA 광고 수익은 개정 약관 동의가 정산 조건입니다)
