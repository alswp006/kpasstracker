# 콘솔 MCP 승인 대본 — KPassTracker

> 콘솔 MCP(2026-07-10)를 Claude에 연결하면 화면을 클릭하지 않고 **자연어로** 워크스페이스·미니앱 관리,
> **검수 신청**, 번들 배포, 통계 조회를 할 수 있습니다. 현재 **Claude만 지원**(Codex 준비 중).
> 아래는 이 앱의 값을 **미리 박아 둔** 대본입니다 — 편집하지 말고, 확인하고, 그대로 붙여 넣으세요.

각 단계는 **① 눈으로 확인 → ② 복붙용 프롬프트 → ③ 다음 단계 기준** 순서입니다.
**되돌릴 수 없는 지점은 4단계(검수 요청) 하나**입니다 — 그 앞까지는 전부 고칠 수 있습니다.

| 단계 | 하는 일 | 되돌리기 |
|---|---|---|
| 0 | MCP 등록 (최초 1회) | — |
| 1 | 앱 등록 (3필드) | appName **불가** · 앱 유형 사실상 불가(재등록) |
| 2 | 출시 전 폼 | 가능 |
| 3 | 에셋 업로드 | 가능 |
| 4 | **검수 요청** | **어려움** |
| 5 | 배포 후 설정 | 가능 |

## 먼저 · 제작자 이름 (워크스페이스 1회 · 설정 여부 **모름**)

> 우리는 콘솔을 볼 수 없어 설정 여부를 확인하지 못했습니다. 이미 정했다면 이 블록을 건너뛰세요.

2026-08-28부터 **앱 상세 화면에 제작자 이름이 노출**됩니다. 앱이 아니라 **워크스페이스 단위**라,
한 번 정하면 이 공장이 낸 **모든 미니앱에 똑같이** 붙고 이름을 누르면 **같은 제작자의 다른 미니앱**이 보입니다
— 여러 앱을 내는 이 공장에는 **공짜 크로스 프로모션**입니다.

- 제약: 공백 포함 10자 이내 · 한글·영문·숫자만(특수문자·이모지 불가) · 타사 브랜드 도용·허위·과장 불가 · **다른 제작자가 쓰고 있는 이름은 불가** · 반영까지 약 10분
- 콘솔 위치: 내 정보 → 제작자 정보

```text
앱인토스 콘솔의 제작자 이름 설정 화면(내 정보 → 제작자 정보)을 열어서 현재 값을 보여줘.
제약은 공백 포함 10자 이내 · 한글·영문·숫자만(특수문자·이모지 불가) · 타사 브랜드 도용·허위·과장 불가 · **다른 제작자가 쓰고 있는 이름은 불가** · 반영까지 약 10분 이고,
이미 값이 있으면 **바꾸지 말고 알려만 줘** — 이 값은 워크스페이스 단위라 이미 낸 모든 미니앱의 표기가 함께 바뀐다.
(기존에 회사명을 입력한 워크스페이스는 회사명이 기본 노출돼 있을 수 있다.)
비어 있을 때만 후보 3개를 제안하고, 내가 고르면 그때 저장해줘.
```

## 0단계 · 최초 1회 — MCP 등록 (이미 했으면 건너뛰세요)

**① 눈으로 확인**
- Claude에서 앱인토스 콘솔 MCP가 연결돼 있는가 — 연결돼 있으면 1단계부터 복붙만 하면 됩니다.
- 등록 명령은 **앱인토스 개발자센터 → 'AI로 콘솔 사용하기'** 문서를 보세요. **우리는 그 명령을 확인하지 못했습니다 — 확인 필요.** 여기 적으면 존재하지 않는 명령을 시키는 꼴이 됩니다.
- 등록은 터미널에서 하고, 그다음 Claude에서 콘솔 계정 인증을 한 번 합니다.

**② 복붙용 프롬프트**
```text
앱인토스 콘솔 MCP가 연결돼 있는지 확인해줘.
연결돼 있으면 내 워크스페이스와 미니앱 목록을 보여주고,
연결돼 있지 않으면 지금 무엇이 없는지만 알려줘(임의로 설치하지는 마).
```

**③ 다음 단계 기준** — 미니앱 목록이 보이면 연결된 것입니다. 한 번 등록하면 이후 단계는 전부 복붙입니다.

## 1단계 · 앱 등록 (3필드)

**① 눈으로 확인**
- **appName이 `kpasstracker`과 정확히 일치하는가.** 한 번 등록하면 **되돌릴 수 없고**, 불일치하면 이후 모든 배포가 **4031**입니다.
- 앱 이름(표시 이름): KPassTracker — 이건 나중에 고칠 수 있습니다.
- 앱 유형: 비게임

**② 복붙용 프롬프트**
```text
앱인토스 콘솔에 새 미니앱을 등록해줘. 값은 아래 그대로 쓰고 바꾸지 마.
- 앱 이름: KPassTracker
- appName: kpasstracker
- 앱 유형: 비게임
등록 전에 위 세 값을 그대로 다시 보여주고, 내가 "진행"이라고 답하면 그때 등록해줘.
```

**③ 다음 단계 기준** — 콘솔 목록에 이 미니앱이 보이고 appName이 위 값과 같으면 통과. 등록이 `ait deploy`의 **전제조건**입니다.

## 2단계 · 출시 전 폼 (카테고리·부제·상세 설명·검색 키워드·주요 기능)

**① 눈으로 확인**
- 카테고리: **생활** (매니페스트 값).
- 2026-08-27 **'재테크' 카테고리가 신설**됐습니다(가계부·소비 분석·절약 챌린지·저축 습관 관리·투자 기록·모의 투자·경제/금융 지식 — 전용 탭에서 관심 유저에게 노출). 바꾸려면 콘솔: 앱 관리 → 앱 정보 수정 → 카테고리. 이 앱이 위 예시 중 하나라면 '재테크'가 맞을 수 있습니다 — **판단은 사람이** 하세요.
- 나머지 칸은 `artifacts/apps-in-toss-manifest.txt`의 **[2스텝]** 블록을 그대로 씁니다(같은 값이 SUBMIT.md 1번과 같은 원천).
- 이 칸들은 **유저가 앱을 실행하기 전에** 상세 페이지에서 읽는 문구입니다(심사 서류가 아닙니다).

**② 복붙용 프롬프트**
```text
앱인토스 콘솔에서 미니앱 `kpasstracker`의 출시 전 정보를 채워줘.
- 카테고리: 생활
- 부제 / 상세 설명 / 검색 키워드 / 주요 기능: 내가 붙여 넣는 [2스텝] 블록 그대로
저장하기 전에 채울 값을 전부 보여주고, 내가 "저장"이라고 답하면 그때 저장해줘.
```

**③ 다음 단계 기준** — 콘솔 상세 페이지 미리보기에 부제·설명·주요 기능이 다 보이면 통과.

## 3단계 · 에셋 (로고·배너·스크린샷)

**① 눈으로 확인**
- 준비됨: 로고 `artifacts/logo.png` · 다크 `artifacts/logo-dark.png`
- 준비됨: 배너 `artifacts/thumbnail-wide.png`
- 준비됨: 스크린샷 `artifacts/screenshots/` 4장(세로 3·가로 1) (콘솔 규격 확인됨)
- **스크린샷이 자리 페이지("준비 중")나 흰 화면이 아닌지** 눈으로 확인하세요 — 자동 캡처는 후보일 뿐입니다.
- 콘솔 최소 요건은 세로 3장 + 가로 1장입니다. 모자라면 직접 캡처해야 합니다.

**② 복붙용 프롬프트**
```text
앱인토스 콘솔의 미니앱 `kpasstracker`에 에셋을 올려줘.
- 로고 `artifacts/logo.png` · 다크 `artifacts/logo-dark.png`
- 배너 `artifacts/thumbnail-wide.png`
- 스크린샷 `artifacts/screenshots/` 4장(세로 3·가로 1) (콘솔 규격 확인됨)
올리기 전에 어떤 파일이 어느 칸에 들어가는지 보여주고, 내가 "올려"라고 답하면 그때 업로드해줘.
```

**③ 다음 단계 기준** — 콘솔 미리보기에서 로고·배너·스크린샷이 의도한 화면으로 보이면 통과.

## 4단계 · 검수 요청 — **되돌리기 어려운 유일한 지점**

> ⛔ **여기서 멈추고 QR(테스트 스킴)로 폰에서 직접 열어 보세요.** 미리보기가 초록이어도 폰에서 흰 화면인
> 경우가 실제로 있었습니다(라우터 basename·벤더 SDK). 사람이 폰에서 한 번 여는 것이 마지막 그물입니다.
> 아직 배포되지 않아 테스트 스킴이 없습니다 — `ait deploy` 후 콘솔 '테스트하기' QR로 확인하세요.

**① 눈으로 확인**
- 폰에서 QR로 열어 **핵심 여정 한 번**을 끝까지 해봤는가.
- 뒤로가기·닫기 버튼이 **동작**하는가, 토스 내비게이션 바의 뒤로가기와 **중복 노출**되지 않는가([9-10] 반복 위반 사례).
- **테스트 광고 키·테스트 프로모션 키가 남아 있지 않은가**([9-10] 반복 위반 사례).
- 약관/법적 체크박스는 **사람이** 직접 동의해야 합니다(자동화 금지).

**② 복붙용 프롬프트**
```text
앱인토스 콘솔에서 미니앱 `kpasstracker`의 검수를 신청해줘.
신청 전에 등록된 정보(앱 이름·appName·앱 유형·카테고리·부제·설명·에셋)를 요약해서 보여주고,
빠졌거나 이상한 칸이 있으면 먼저 알려줘.
내가 "검수 신청"이라고 정확히 답하면 그때만 신청해줘.
```

**③ 다음 단계 기준** — 콘솔 상태가 '검수 중'으로 바뀌면 통과.

## 5단계 · 배포 후 콘솔 설정 (수익·유통)

**① 눈으로 확인**
- 아래는 `ait deploy` **후**에 하는 설정입니다 — 제출 전(1~4단계)과 시점이 다릅니다.
- **핵심 지표 → 활성 지표 정의**(재방문·체류시간·방문 횟수) — 미설정이면 토스가 활성 유저의 **유사 유저에게 앱을 추천**하지 않습니다(광고비를 안 쓰는 이 공장의 **유일한 무료 유통 스위치**가 꺼진 채입니다). **전환 지표 템플릿도 재선택 후 저장**하세요(일부 항목이 활성 지표로 이관됐습니다). 변경은 **다음 날부터** 적용됩니다.
- **인앱 광고 분석** — 요청→수신→노출시도→노출성공 4단 퍼널과 유형별 **eCPM**을 확인합니다. **SDK 2.7.0 이상**이어야 하고, 업데이트 **이후** 데이터부터 수집됩니다(그 전 구간은 비어 있는 게 정상입니다).
- **광고 그룹/슬롯 ID 발급 후 env 주입 재빌드** — 발급값은 빌드 시점에 앱에 박힙니다. 콘솔에서 발급만 받고 끝내면 앱은 그 값을 모릅니다 — **env 주입 후 재빌드·재배포**가 필요합니다.
- **구독(IAP 정기 결제)을 쓴다면 webhook URL 등록**(필수) — **샌드박스가 없으므로** 실제 결제로 확인해야 합니다.
- **사용자 정보 불러오기(토스 로그인)를 쓴다면** 콘솔에서 동의 화면을 설정하고 코드를 발급받으세요(한 번 요청에 **최대 5개** 항목). — 다만 **정말 필요한지 먼저 확인하세요**: `anonKey`는 서버 연동·동의 절차 **없이** 미니앱 안에서 바로 발급되고(미니앱마다 고유 · 같은 사용자는 항상 같은 값 · 검증 API 제공) 스마트발송·프로모션·토스페이에 그대로 쓸 수 있습니다. 토스 로그인이 필요한 경우는 **본인 확인·중복가입 방지처럼 앱인토스 밖 계정과 같은 사람인지 연결해야 할 때뿐**이고, 동의 화면 이탈률이 **절반에 가까운** 미니앱도 있었습니다(2026-07-23).

**② 복붙용 프롬프트**
```text
앱인토스 콘솔에서 미니앱 `kpasstracker`의 배포 후 설정을 점검해줘.
1) 활성 지표가 정의돼 있는지, 전환 지표 템플릿이 재선택·저장돼 있는지
2) 인앱 광고의 요청→수신→노출시도→노출성공 4단 퍼널과 유형별 eCPM
3) 최근 7일 통계 요약
바꿀 것이 있으면 제안만 하고, 내가 승인하면 그때 바꿔줘.
```

**③ 다음 단계 기준** — 활성 지표가 설정돼 있고 통계가 보이면 통과. 지표 변경은 **다음 날부터** 반영됩니다.

---

같은 사실을 사람이 클릭으로 하는 순서는 `artifacts/submission/SUBMIT.md`에 있습니다(A·B·C절).
두 문서의 **배포 후 설정**은 같은 상수에서 나옵니다 — 어긋나면 그건 버그입니다.
